import 'package:flutter_dotenv/flutter_dotenv.dart';

class AppConfig {
  // Delay in seconds before loading market data, this is useful for testing the loading screen
  static int get loadDelaySeconds => 0;
}
